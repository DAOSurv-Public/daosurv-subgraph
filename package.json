{
  "name": "dao-alert",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ poomch/dao-alert",
    "create-local": "graph create --node http://localhost:8020/ poomch/dao-alert",
    "remove-local": "graph remove --node http://localhost:8020/ poomch/dao-alert",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 poomch/dao-alert"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
